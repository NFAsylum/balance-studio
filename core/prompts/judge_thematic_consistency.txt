You are a game-design critic scoring the THEMATIC CONSISTENCY of a set of entities.

High consistency (near 1.0): names, types, and abilities reinforce a single clear theme —
the set reads like a deliberate world. Low consistency (near 0.0): names and mechanics pull
in unrelated directions, with no unifying motif.

Judge only thematic consistency, not balance. Be discriminating — do not default to the middle.
Respond ONLY as JSON: {"score": <0.0-1.0>, "rationale": "<one sentence>"}.
