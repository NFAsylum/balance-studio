> **Note:** This document discusses design considerations that reference commercial games by name for comparative and structural purposes. See README `## Legal` section for full non-affiliation disclosure.

# INBOX — 2026-07-13 (Balance Studio — Review do PR #9)

Após executar, mova pra `docs/inbox-archive/2026-07-13-review-pr9.md`.

## Contexto

Review rigorosa do PR #9 (`chore: IP hygiene`) identificou **4 bloqueadores** e **1 gap não-bloqueante**. Execução dessa trilha desbloqueia o merge do PR.

**Estado do PR agora:** `mergeable_state=dirty` (conflito com main), branch `dev-marco-ip-hygiene`, 1 commit (`51da225`).

---

## Red lines (não-negociáveis)

- ❌ **`git rebase` proibido** — CLAUDE.md global e T-IP.9. Se conflito exigir mais que `git merge`, escale.
- ❌ **Sem force push, sem filter-repo, sem history rewrite.** Já eram red lines em T-IP.9 e continuam.
- ❌ **Não postar comentário no GitHub sem confirmação humana.**

---

## Ordem sequencial

Todas as tasks têm DoD verificável. Execute na ordem.

### T-REV.1 [10min] Sync com main via merge (não rebase)

```bash
git checkout dev-marco-ip-hygiene
git fetch origin
git merge origin/main            # NÃO usar rebase
# resolver conflitos manualmente se houver
git commit                       # merge commit padrão, sem --no-verify
git push
```

Se aparecer conflito em arquivo que **não** é preset/prompt/doc de IP (ex.: main mudou `iteration_engine.py`), resolva preservando ambas as mudanças com bom senso. Se conflito for em arquivo IP-sensível (preset JSON, `llm_local.py::_SYSTEM`), **escale** — não decida sozinho qual versão prevalece.

**DoD:**
- `gh pr view 9 --repo NFAsylum/balance-studio --json mergeable` retorna `"mergeable":true` (ou `"MERGEABLE"`)
- Nenhum commit foi reescrito (`git log --oneline` mostra `51da225` intacto + merge commit em cima)
- `pytest tests/` verde no local pós-merge

### T-REV.2 [15min] Verificar auditoria contra main atual

O PR body afirma que PRs #4/#5/#6 (features f3/f4/f5) precisariam do mesmo tratamento. `gh pr list --state open` retorna **só o #9**. Duas leituras:

- **(a)** PRs já mergeados em main → main atual pode ter example entities com nomes tipo "Charizard", "Blue-Eyes", "Serra Angel". A auditoria do PR #9 branchou de main mas focou só nos presets renomeados.
- **(b)** PRs foram fechados sem merge → flag do body é aspiracional/stale e o texto está errado.

**Execute:**

```bash
# grep exaustivo em main pós-features contra lista de trademarks
git checkout main
git pull
grep -rInE "Charizard|Blue-Eyes|Blue.Eyed|Serra Angel|Lightning Bolt|Pikachu|Bulbasaur|Squirtle|Onyx|Snorlax|Mewtwo|Dark Magician|Exodia|Kuriboh|Reaper King|Zerg|Diablo|Skyrim|Elden Ring|Cyberpunk 2077|Overwatch|Warcraft|Ironclad|Silent|Defect|Watcher" \
  --include="*.py" --include="*.json" --include="*.md" --include="*.tsx" --include="*.ts" \
  ':!docs/inbox-archive/' ':!docs/product-audit.md' ':!docs/questions-archive/'
```

Se retornar matches em `presets/*/entities/*.json`, `tests/`, `core/`: **descobriu-se novo escopo de cleanup** que o PR #9 não cobriu. Documente em `docs/questions-archive/2026-07-13-q2-post-pr9-audit.md` a lista completa e **escale** — o humano decide se PR #9 absorve os fixes ou se vira PR follow-up.

Se retornar zero: atualize o body do PR #9 removendo a flag #3 stale.

**DoD:**
- Grep executado documentado (arquivo com output ou "zero matches")
- Se matches: `docs/questions-archive/2026-07-13-q2-post-pr9-audit.md` criado + escalação
- Se zero: PR body editado via `gh pr edit 9 --body ...` removendo/corrigindo flag #3

### T-REV.3 [2min] Disclaimer no `docs/questions-archive/2026-07-13-q1-categorical-fidelity.md`

Arquivo contém 6 refs trademark diretas (YuGiOh, Hearthstone, Pokémon) mas não recebeu a linha de T-IP.8.

Adicionar no **topo** (antes do `# Questions for the human`):

```markdown
> **Note:** This document discusses design considerations that reference commercial games by name for comparative and structural purposes. See README `## Legal` section for full non-affiliation disclosure.
```

**DoD:**
- Linha existe no arquivo, no topo, sem alterar mais nada
- `grep -c "This document discusses design considerations" docs/questions-archive/2026-07-13-q1-categorical-fidelity.md` retorna `1`

### T-REV.4 [30min] Renomear visual variants (upgraded de LOW → HIGH)

**Racional:** análise do nominative fair use (New Kids v. News America 1992) mostra que os IDs falham prongs 1 e 2 do teste:

- **(1) Produto identificável sem trademark?** Falha — `modern-mana` funciona idêntico a `hearthstone` como identificador.
- **(2) Uso mínimo necessário?** Falha — o identificador *inteiro* é a trademark.

Adicionalmente: nomes de arquivo `.tsx` são indexados pelo Google (repo público MIT); LangChain/Machinations/SillyTavern não usam trademarks como IDs de plugin. A T-IP.9 do inbox original tinha trocado exposição por custo de refactor, mas o custo é ~30min — desproporcional pequeno pra manter exposição real.

**Renomes:**

| Atual | Proposto |
|---|---|
| `card_game.hearthstone` | `card_game.modern-mana` |
| `card_game.yugioh` | `card_game.high-scale-duel` |
| `creature_rpg.pokedex` | `creature_rpg.elemental-classic` |
| `creature_rpg.monster-hunter` | `creature_rpg.giant-beast` |

**Passos:**

1. Renomear os arquivos `.tsx` correspondentes (visual variants em `ui/`):
   ```bash
   git mv ui/.../HearthstoneStyle.tsx ui/.../ModernManaStyle.tsx
   git mv ui/.../YuGiOhStyle.tsx      ui/.../HighScaleDuelStyle.tsx
   git mv ui/.../PokedexStyle.tsx     ui/.../ElementalClassicStyle.tsx
   git mv ui/.../MonsterHunterStyle.tsx ui/.../GiantBeastStyle.tsx
   ```
   Localizar caminho exato via `find ui/ -name "*Style.tsx"` ou equivalente.

2. Renomear as classes/componentes exportados no conteúdo dos arquivos (`HearthstoneStyle` → `ModernManaStyle`, etc.).

3. Atualizar o view registry (arquivo tipo `ui/config/domain-views.ts` ou equivalente) — trocar chaves e imports.

4. Atualizar `default_visual_variant` nos 6 presets:
   - `presets/card_game/modern-mana-tcg.json`: `"card_game.hearthstone"` → `"card_game.modern-mana"`
   - `presets/card_game/high-scale-duel.json`: `"card_game.yugioh"` → `"card_game.high-scale-duel"`
   - `presets/creature_rpg/elemental-creatures-classic.json`: `"creature_rpg.pokedex"` → `"creature_rpg.elemental-classic"`
   - `presets/creature_rpg/giant-beast-hunt.json`: `"creature_rpg.monster-hunter"` → `"creature_rpg.giant-beast"`

5. Atualizar qualquer teste que referencie os IDs antigos (grep).

6. Commit único: `chore: rename visual variants — nominative fair use compliance (follow-up to T-IP.9)`.

**DoD:**
- `grep -rE "hearthstone|yugioh|pokedex|monster-hunter" --include="*.tsx" --include="*.ts" --include="*.json" --include="*.py"` fora de `docs/inbox-archive/`, `docs/product-audit.md`, `docs/questions-archive/`, README `## Legal`, e do system prompt `_SYSTEM` retorna **zero matches** em contexto de identificador
- `pytest tests/` verde
- Frontend build limpo (`cd ui && npm run build`) — sem componente órfão

### T-REV.5 [10min] Migration note pro breaking change do enum MTG (opcional-forte)

`ability_kind` mudou de `burn/lifegain/counter/cantrip` → `direct_damage/restore_life/negate/card_draw`. Se algum event log persistido usar valores antigos, deserialização quebra.

Escolha uma das duas:

- **(a)** Adicionar teste `tests/test_migration_v0_1_to_v0_2.py` que carrega scenario fixture com enum antigo e falha com erro claro ("scenario X uses pre-v0.2 MTG enum; recreate or migrate").
- **(b)** Adicionar `CHANGELOG.md` com seção `## v0.2-ip-clean (2026-07-13)` documentando o breaking change e a instrução de recriar cenários MTG.

**(b) é mais leve e suficiente pra um repo pré-usuários.**

**DoD:**
- (a) ou (b) implementado
- Commit contém referência ao breaking change

---

## Depois de tudo verde

1. `git push origin dev-marco-ip-hygiene`
2. Verificar `gh pr view 9 --json mergeable` = `MERGEABLE`
3. **Aguardar autorização humana** pra:
   - Postar comentário no PR resumindo o que foi endereçado
   - Fazer o merge
   - Criar tag anotada `v0.2-ip-clean` no merge commit (T-IP.7 original)

**Não faça nenhum dos 3 acima sem confirmação explícita do humano.** É review + tag + merge — high-visibility.

---

## Diretiva sobre escopo

Execute T-REV.1 → T-REV.4 na ordem. T-REV.5 opcional-forte (recomendo (b), 5min).

Se travar em qualquer ponto (conflito não-trivial no merge, arquivo `.tsx` não localizado no lugar esperado, teste que falha por razão obscura), **escale imediatamente**. Nunca decida corte sozinho — precedente do inbox anterior mostra que preemptive cuts levam a under-delivery.

Reporte no próximo prompt:
- SHA do merge commit (T-REV.1)
- Output do grep de T-REV.2
- Confirmação de que T-REV.3 e T-REV.4 têm DoD verde
- Escolha de T-REV.5 (a/b)